[Events](/docs/concepts/events) are the raw log records emitted by SDKs — every `Pulse.info()`, `Pulse.error()`, `Pulse.step()`, etc. Filter by app, level, user, session, environment, screen, or time range. Defaults to the last 24 hours.
