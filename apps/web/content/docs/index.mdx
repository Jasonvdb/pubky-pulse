---
title: Owlmetry Documentation
description: Self-hosted observability platform for web, backend and mobile apps. SDKs for Swift, Kotlin and Node.js, an MCP endpoint for agents, and a REST API.
---

Owlmetry is a self-hosted observability platform built for the agentic development era. It captures events, metrics, and funnels from your apps and backend services, stores everything in a single PostgreSQL instance, and exposes it all through a REST API, an MCP endpoint, and an optional web dashboard.

Your agent can set up the entire integration, query events, diagnose issues, and act on what it finds -- without a human opening a dashboard.

## Key Features

- **Event ingestion** with batch processing, deduplication, and gzip compression
- **Structured metrics** with lifecycle tracking (start/complete/fail) and aggregation queries
- **Funnel analytics** to measure conversion and drop-off across multi-step flows
- **Issue tracker** that clusters error events into actionable issues with regression detection
- **Event attachments** for uploading logs, screenshots, and crash dumps alongside error events
- **Agent-native API** where every operation is available through `owl_agent_` keys
- **Single PostgreSQL** backend with monthly-partitioned event tables -- no Kafka, no ClickHouse, no Redis

## Automate everything

The web dashboard is great for exploring data, but you shouldn't need to open it for routine work. Owlmetry exposes every operation -- creating projects, defining metrics, querying funnels, triaging issues -- through its MCP endpoint. **MCP is the agent interface** -- configure it once and your agent always has the latest tools. Your agent can set up an entire analytics pipeline, investigate production issues, and act on the results without a human ever touching the dashboard.

- **Setup** — a one-time URL + API key in your client config
- **Updates** — automatic; the tool list always matches the server
- **Auth** — a static Bearer token in the request headers
- **Best for** — AI coding agents (Claude Code, Cursor, Codex, and any other MCP-compatible client)
- **Requires** — an MCP-compatible client

For scripts and CI/CD pipelines that can't speak MCP, the [REST API](/docs/api-reference) covers the same operations with the same `owl_agent_` keys.

Learn more: [MCP setup](/docs/mcp/setup)

## Documentation Sections

- **[Getting Started](/docs/getting-started)** -- Set up your first project and start sending events in minutes. Connect your agent via MCP, then add an SDK.
- **[Concepts](/docs/concepts)** -- Core building blocks: [events](/docs/concepts/events), [metrics](/docs/concepts/metrics), [funnels](/docs/concepts/funnels), [projects and apps](/docs/concepts/projects-and-apps), [authentication](/docs/concepts/authentication), and [data mode](/docs/concepts/data-mode).
- **[Swift SDK](/docs/sdks/swift)** -- Native SDK for iOS, iPadOS, and macOS. Zero external dependencies. Covers events, screen tracking, metrics, funnels, and identity.
- **[Android SDK](/docs/sdks/android)** -- Kotlin SDK for Android, with optional Jetpack Compose UI. Covers events, screen tracking, metrics, funnels, identity, feedback, and questionnaires.
- **[Node.js SDK](/docs/sdks/node)** -- Server-side SDK for backends and serverless functions. Zero runtime dependencies. Covers events, metrics, funnels, identity, and serverless support.
- **[MCP](/docs/mcp)** -- Connect AI agents directly to Owlmetry via the Model Context Protocol. The single agent interface: no installation, automatic updates, 62 tools covering every operation.
- **[API Reference](/docs/api-reference)** -- Complete REST API covering authentication, ingestion, querying, project/app management, metrics, funnels, issues, attachments, teams, and identity.
- **[Self-Hosting](/docs/self-hosting)** -- Deploy on your own infrastructure. Covers system requirements, PostgreSQL, nginx, SSL, pm2, and maintenance.
