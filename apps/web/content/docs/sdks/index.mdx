---
title: SDKs
description: Client SDKs for sending events, metrics, and funnels to Pubky Pulse from web, backend, and mobile apps.
---

> The SDK repositories are being rebuilt under the [pubky](https://github.com/pubky) org. Package names, coordinates, and version numbers on these pages may still change.

The SDKs send events, metrics, and funnels to Pubky Pulse from web, backend, and mobile apps. They are lightweight with minimal dependencies, and handle batching, gzip compression, retry with exponential backoff, and automatic lifecycle events out of the box.

## Available SDKs

- **[Web SDK](/docs/sdks/web)** — *coming soon.* Browser-side event, metric, and funnel capture for web apps.
- **[Node.js SDK](/docs/sdks/node)** — Server-side SDK for Express, Fastify, and serverless functions. Pure ESM package, zero runtime dependencies. Covers event logging, structured metrics, funnels, identity scoping, and serverless support.
- **[Swift SDK](/docs/sdks/swift)** — Native SDK for iOS, iPadOS, macOS, and watchOS apps. Distributed as a Swift Package, zero external dependencies. Covers event logging, screen tracking, structured metrics, funnels, identity management, network monitoring, and app launch time measurement.
- **[Android SDK](/docs/sdks/android)** — Kotlin SDK for Android apps, with optional Jetpack Compose UI. Published to Maven Central; the core module has a single dependency (`kotlinx-coroutines`). Covers event logging, screen tracking, structured metrics, funnels, identity management, feedback, and questionnaires.

## Comparison

The table covers the SDKs that ship today; the Web SDK is not in it yet.

| Feature | Node.js | Swift | Android |
|---------|---------|-------|---------|
| Events with log levels | Yes | Yes | Yes |
| Structured metrics | Yes | Yes | Yes |
| Funnel tracking | Yes | Yes | Yes |
| Auto screen tracking | — | Yes | Yes (Compose) |
| In-app feedback / questionnaires | — | Yes | Yes (Compose) |
| Network monitoring | — | Yes | — |
| App launch time | — | Yes | Yes |
| Serverless support | Yes | — | — |
| User scoping | `Pulse.withUser()` | `Pulse.setUser()` | `Pulse.setUser()` |
| Compression | Gzip | Gzip | Gzip |
| Min runtime | Node.js 20 | iOS 16 / macOS 13 | Android 7 (API 24) |

## Choosing an SDK

- Building a **web app** → the [Web SDK](/docs/sdks/web) is coming soon. Until it lands, send events straight to the [ingest API](/docs/api-reference/ingest) or route them through your own backend with the Node.js SDK.
- Building a **backend service, API, or serverless function** → [Node.js SDK](/docs/sdks/node)
- Building an **iOS, iPadOS, macOS, or watchOS** app → [Swift SDK](/docs/sdks/swift)
- Building an **Android** app → [Android SDK](/docs/sdks/android)
- Building across **web, backend, and mobile** → use several. Events from every platform appear in the same project dashboard.
