---
title: Privacy & App Store compliance
description: What ships in the SDK, what you need to declare in your app's App Privacy settings, and how Pubky Pulse handles tracking, IDFA, and required-reason APIs.
---

The Pubky Pulse Swift SDK ships an Apple-compliant `PrivacyInfo.xcprivacy` manifest and never requires an App Tracking Transparency (ATT) prompt. This page lists what you need to do in your app's App Privacy settings on your next submission — typically a one-time, five-minute task — and what carries forward unchanged after that.

## What we ship

The SDK bundles `PrivacyInfo.xcprivacy` as a resource. SPM merges it into your app at build time — there's nothing to import, configure, or call. When you archive your app in Xcode, the Organizer's **Generate Privacy Report** action aggregates Pubky Pulse's declarations into your app's combined Privacy Report PDF alongside any other SDKs you use.

You do not need to edit `Package.swift`, copy a file, or run a script.

## App Privacy Nutrition Label

The privacy manifest and the in-store Privacy Nutrition Label are **separate systems**. Apple does not auto-sync them — the manifest is for static analysis at submission, the Nutrition Label is what users see on your App Store listing. On your next submission, update your app's **App Privacy** settings to tick the categories below.

| Data type | Linked to User? | Used for tracking? | Purpose |
|---|---|---|---|
| Crash Data | No | No | App Functionality, Analytics |
| Other Diagnostic Data | No | No | App Functionality, Analytics |
| Product Interaction | No | No | Analytics, App Functionality |
| Performance Data | No | No | App Functionality, Analytics |
| Other User Content | No | No | App Functionality, Analytics |
| User ID | **Yes** *(if you call `Pulse.setUser`)* | No | App Functionality, Analytics |

These are exactly what Pubky Pulse's manifest declares. The categories cover error/crash events, lifecycle and event logs, screen views and funnel/metric events, network instrumentation timings, custom event attributes, and free-text feedback bodies.

## Conditional declarations

A few features add categories you should declare yourself in addition to the table above.

### Calling `Pulse.setUser`

`Pulse.setUser(_:)` ties the SDK's anonymous ID to a real user identifier you supply. Declare **User ID** as **Linked to User** in your Nutrition Label.

### Using `PulseFeedbackView` with contact fields

`PulseFeedbackView` is a SwiftUI form that lets users submit free-text feedback. With `showsContactFields: true` (the default), the form renders optional **Name** and **Email Address** inputs that the user types in themselves. These fields are user-initiated and opt-in, so Pubky Pulse's SDK manifest does not declare them — but if you ship this UI in your app, you are collecting that data and should add to your Nutrition Label:

- **Email Address** — Linked to User, App Functionality, Customer Support
- **Name** — Linked to User, App Functionality, Customer Support

If you instead set `showsContactFields: false`, or call `Pulse.sendFeedback(_:)` programmatically without forwarding contact info, neither needs to be declared.

## Where your data goes

The Nutrition Label rows above describe *what* the SDK collects. *Where* it ends up is settled by your `Pulse.configure` call: events reach exactly one recipient, the Pubky Pulse instance behind the `endpoint` in effect. One destination is not the same as a destination you own, though, and who operates it is what the rest of your answers turn on:

- **You run that instance.** Events go only to infrastructure you control — a first-party destination.
- **Someone else runs it.** [Pubky's hosted instance](/docs/getting-started/hosted) and any other managed Pubky Pulse provider are separate companies from you; name one in `endpoint` and it is the recipient.

`endpoint` is optional, and that is how you land in the second case without meaning to: omit it and the SDK falls back to Pubky's hosted instance. The fallback is **silent** — nothing is logged, warned, or thrown — so pass your own ingest host explicitly in `Pulse.configure` if you want to be sure you are in the first case.

Either way, Apple asks you to declare data collected by a third-party SDK as your *own* collection, so the table above stands unchanged — and so do the tracking answers below, since neither destination links your users' data to third-party data for advertising or hands it to a data broker. What does change is your privacy policy and any data-deletion commitment you make: on a backend you run you can purge a user's events server-side, so describe that path and point users at it, while on an instance you do not run the data is not yours to purge — name the real recipient and describe the deletion path you actually have. And wherever a store, partner, or compliance questionnaire asks who receives the data, answer for whoever operates the instance the SDK sends to.

## App Tracking Transparency

The SDK does **not** require an ATT prompt:

- We do **not** access the IDFA (`advertisingIdentifier`) and do not link against the `AdSupport` framework.
- We do **not** access `identifierForVendor` (IDFV).
- We use an SDK-owned anonymous identifier (`pulse_anon_*`) stored only in the device Keychain (scoped to this app and this device) — never shared across apps. The real user identifier you supply via `Pulse.setUser` is stored in your app's UserDefaults.

If you do not show an ATT prompt for other reasons, Pubky Pulse will not change that. If you do show one for other SDKs, Pubky Pulse continues to work whether the user grants or denies tracking — there is no separate user-facing prompt for Pubky Pulse.

## Required-reason APIs

Apple requires SDKs and apps to declare any use of certain "required-reason" APIs in their privacy manifest. Pubky Pulse declares the one it uses:

| API category | Reason code | Where |
|---|---|---|
| `NSPrivacyAccessedAPICategoryUserDefaults` | `CA92.1` | Persist the real user identifier (set via `Pulse.setUser`) in your app's own UserDefaults suite |

We do not access file timestamp APIs, system boot-time APIs, disk-space APIs, or active-keyboard APIs.

**You still need your own manifest.** If your app uses any required-reason APIs directly — and most apps do, since `UserDefaults` is on the list — you need a `PrivacyInfo.xcprivacy` file at the root of your app target declaring your own usage. Pubky Pulse's manifest only covers Pubky Pulse's usage.

## Privacy policy template

Apple requires a privacy policy URL on your App Store listing. If your current policy does not already mention third-party analytics, paste a paragraph like this:

> We use Pubky Pulse, a self-hosted analytics service, to understand how users interact with our app and to diagnose errors. Pubky Pulse collects diagnostic data (including crash reports and event logs), product interaction data (such as screen views), and any user feedback you choose to submit. If you sign in, your user identifier is linked to this data so we can support you and improve the product. Pubky Pulse does not use the data for cross-app tracking or advertising and does not access your device's advertising identifier (IDFA).

Adjust to match your app's voice and the specific Pubky Pulse features you use. "Self-hosted" only fits if you operate the instance the SDK sends to; if `endpoint` points at Pubky's hosted instance or another provider's — or is left out, which takes the hosted default — drop the word and name the real recipient.

## What carries forward

After the one-time Nutrition Label update on your next submission, every subsequent submission is unchanged. The manifest ships with the SDK and updates automatically when you upgrade Pubky Pulse. You only need to revisit your app's App Privacy settings if you start using a new Pubky Pulse feature that introduces a category you have not yet declared (for example, enabling `PulseFeedbackView` contact fields for the first time).
